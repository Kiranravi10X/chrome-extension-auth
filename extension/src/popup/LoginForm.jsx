import React, { useEffect } from 'react';

const LoginForm = () => {
  // useEffect(() => {
  //   chrome.storage.sync.get(["token"], (data) => {
  //     if (data.token) {
  //       console.log("User is authenticated with token:", data.token);
  //     } else {
  //       console.log("User is not logged in");
  //     }
  //   });
  // }, []);

  return (
    <div>User Page token</div>
  );
}

export default LoginForm;